---
- hosts: localhost
  become: yes
  tasks:
    - name: Installation du serveur Apache
      apt:
        name: apache2
        state: present
        update_cache: yes
    - name: Activation du service Apache
      service:
        name: apache2
        state: started
        enabled: yes